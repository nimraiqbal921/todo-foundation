# AI Tool Calling Chat App

This is a [Next.js](https://nextjs.org) project built with the AI SDK and Groq AI.

The application demonstrates a streaming AI chat interface with server-side tool calling, structured tool outputs, and custom UI components.

---

## Getting Started

First, install dependencies:

```bash
npm install